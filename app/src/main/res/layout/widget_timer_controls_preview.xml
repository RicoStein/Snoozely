<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Statischer, abgerundeter, blauer Hintergrund -->
    <ImageView
        android:id="@+id/bg_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:contentDescription="background"
        android:scaleType="fitXY"
        android:src="@drawable/bg_widget_preview" />

    <!-- Inhalt -->
    <LinearLayout
        android:id="@+id/content_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingLeft="12dp"
        android:paddingRight="12dp">

        <ImageView
            android:id="@+id/imgIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="logo icon"
            android:src="@drawable/ic_app_logo"
            app:tint="@color/widget_preview_text" />

        <TextView
            android:id="@+id/txtTime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:paddingLeft="8dp"
            android:paddingRight="4dp"
            android:maxLines="1"
            android:singleLine="true"
            android:ellipsize="none"
            android:text="15:00"
            android:textColor="@color/widget_preview_text"
            android:textStyle="bold"
            android:textSize="18sp" />

        <ImageView
            android:id="@+id/btnMinusIv"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="minus"
            android:paddingLeft="8dp"
            android:src="@drawable/ic_minus_24"
            app:tint="@color/widget_preview_text" />

        <ImageView
            android:id="@+id/btnPlayIv"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="play"
            android:paddingLeft="8dp"
            android:src="@drawable/ic_play_24"
            app:tint="@color/widget_preview_text" />

        <ImageView
            android:id="@+id/btnPlusIv"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="plus"
            android:paddingLeft="8dp"
            android:src="@drawable/ic_plus_24"
            app:tint="@color/widget_preview_text" />
    </LinearLayout>
</RelativeLayout>
