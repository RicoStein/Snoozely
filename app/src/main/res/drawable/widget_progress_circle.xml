<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hintergrundkreis (der leere "Track") -->
    <item android:id="@android:id/background">
        <shape
            android:shape="ring"
            android:innerRadiusRatio="2.5"
            android:thicknessRatio="15.0"
            android:useLevel="false">
            <solid android:color="#4DFFFFFF" /> <!-- Leicht transparentes Weiß -->
        </shape>
    </item>

    <!-- Vordergrundkreis (der eigentliche Fortschritt) -->
    <item android:id="@android:id/progress">
        <!-- Wichtig: useLevel="true" ist entscheidend für die ProgressBar -->
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270">
            <shape
                android:shape="ring"
                android:innerRadiusRatio="2.5"
                android:thicknessRatio="15.0"
                android:useLevel="true">
                <solid android:color="#FFFFFF" /> <!-- Vollton Weiß -->
            </shape>
        </rotate>
    </item>
</layer-list>
